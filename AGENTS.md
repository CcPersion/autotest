# 通用开发规范

## 当前产品基线

- 当前唯一需求文档是 `docs/requirements/01-产品需求规格说明书.md`，当前唯一任务文档是 `docs/tasks/01-开发任务清单.md`；旧需求、旧任务矩阵和旧审查报告均已废弃。
- 产品采用“自研平台 + Apache JMeter 5.6.3 执行内核”。平台资产使用自己的结构化模型，Runner 运行时生成临时 JMX；不得恢复纯 Java 自研 HTTP/SQL 执行引擎。
- 首版只有一个登录用户类型和一个 Runner；不增加 RBAC、多 Runner 租约、fencing、Kafka、Kubernetes、性能压测或复杂版本中心。
- 架构、数据模型和公共契约变更使用 Sol 高模型审查；已确认任务的编码、测试和 Bug 修复使用 Luna 极高模型。

## 已确认架构后的开发策略

- 架构、需求和任务已经确认后，直接按 `docs/requirements/01-产品需求规格说明书.md` 与 `docs/tasks/01-开发任务清单.md` 执行，不重新设计、不重复论证、不恢复已废弃方案。
- 当前目标是尽快完成需求功能和可运行闭环，不以新增抽象、扩展能力、审查文档数量或测试数量作为进度指标。
- 严格限制范围：当前任务没有要求的页面、接口、依赖、基础设施、扩展点和安全能力不得顺手加入；后续能力必须等任务清单排到对应任务后再实现。
- 一个开发周期只推进一个清晰的纵向功能切片，优先形成“页面/接口 → 持久化 → Runner/JMeter → 报告”的可验证闭环；不要同时铺开多个未收口模块。

### 功能实现优先级

- 开发工作的第一优先级是完成当前需求的代码实现并串通可运行功能闭环；进度按已经接通的功能链路和已完成任务验收数衡量，不按测试数量、审查轮数、日志数量或代码行数衡量。
- 实施顺序固定为：先明确当前切片的入口、调用链和验收边界，随后优先完成页面/接口、持久化、Platform 计划、Runner/JMeter、结果回传和报告中的缺失实现；不要在功能尚未接通前反复运行昂贵的 Compose、Playwright、全量回归或广泛安全扫描。
- 功能代码完成并具备基本可运行条件后，只执行能够直接发现编译错误、公共合同错误和当前调用链断裂的最小定向验证；其余全量测试、真实部署验收、复杂异常矩阵和全面安全回归后置到当前纵向切片串通或阶段收口时一次性执行。
- 真实流程测试、全量测试和专项安全检查不是开发进度本身；除非当前需求明确要求该证据才能判定切片成立，否则不得让它们取代功能实现或阻塞下一段必要代码。涉及安全边界时，只完成需求明确要求的最小检查；不得自行扩展为全面安全扫描、渗透测试或企业级安全验收。
- 发现实现阻塞时，先修复当前功能调用链并继续推进；只有真实阻断当前切片、公共合同或明确安全硬约束的问题才暂停，不因潜在未来风险、无关覆盖率或额外测试建议停工。

### 审查分级

- 架构、数据模型、公共 API、持久化格式、执行语义、安全边界或跨模块合同发生变化时，必须先由 Sol 高模型审查方案，实施后再做一次针对性审查。
- 小范围实现、局部 Bug 修复、测试修正、文档修正、配置调整和不改变合同的重构，不单独启动 Sol 审查；由 Luna 完成修改和相关定向验证，并在所属里程碑统一汇总复核。
- Sol 审查必须只检查本次变更范围和验收条件，不得借审查机会扩展到无关模块、重新设计架构或提出当前任务之外的能力。
- 若小修改实际改变了公共合同、数据结构或安全边界，应立即升级为需要 Sol 审查的变更，不得用“小修改”规避门禁。

### 安全检查分级

- 本产品定位为测试团队内部使用的单用户测试平台，安全是功能边界的配套约束，不是当前开发主目标、进度指标或默认专项；不新增企业级 RBAC、多租户、渗透测试、合规审计或与当前任务无关的安全能力。
- 安全检查采用风险触发，不作为每个普通功能任务的固定步骤；普通页面、普通字段、普通业务逻辑不重复做安全扫描或专项复审。
- 只有本次变更直接涉及登录/Session、密钥和敏感数据、网络目标与代理、SQL/Redis 写操作、文件路径、脚本/扩展执行、反序列化、权限边界或日志脱敏时，才执行一次与变更相关的最小安全检查。
- 安全检查只验证需求中明确的安全约束和本次改动可能影响的调用路径；不因为发现潜在的未来风险就扩展范围，不重复复核已经通过且未改动的安全代码。
- 安全检查发现问题时修复真实阻断；非当前任务、无复现证据或不影响当前边界的建议记录为风险，不阻塞普通功能开发。
- 不得以“安全检查简化”为理由绕过需求明确的密钥不泄露、危险写操作确认、禁止任意脚本和网络白名单等硬性约束。

### 测试与验证策略

- 先运行能够直接证明本次修改正确的最小测试集，再按影响范围补充模块测试、集成测试或真实流程测试。
- 未修改的模块、无关任务和已通过的全量门禁不重复运行；只有阶段收口、跨模块影响、依赖升级或发布前验收才运行一次全量门禁。
- Bug 修复必须重跑原始失败场景和最小相关回归，不因为一次偶然通过就扩大为无关的全量测试。
- WSL、Docker、Testcontainers、JMeter 和 Playwright 真实测试只在本次功能确实涉及时执行；镜像、容器和依赖没有变化时不得反复重建或重复下载。
- 测试失败时先定位根因和调用链，只修复当前失败；禁止为了让门禁变绿而删除有效测试、放宽需求校验或改变既定合同。
- 依赖下载优先复用本地缓存；确需下载时遵守本文件的国内镜像、超时、断点续传和官方哈希校验规则。

### 任务推进和进度记录

- 任务状态只使用：`未开始`、`开发中`、`定向验证中`、`待里程碑验收`、`已完成`、`阻塞`。
- 只有实现、验收条件和必要验证全部具备证据，才标记为`已完成`；写了部分代码或完成首片不能冒充任务完成。
- 复用现有 `progress.md` 和任务文档，不为每个小修复新建重复的计划、矩阵或审查报告。
- 每次交接只报告四项：实际修改、直接验证命令及结果、未验证风险、下一项任务；不重复粘贴无关历史日志。
- Agent 任务必须小而明确，写清负责文件、验收条件和停止边界；完成后立即交付，不继续扩展范围或长时间只做分析。
- 进度优先按“已完成的任务验收数”和“可运行闭环数”汇报，不按代码行数、文件数或首片数量自我美化。

## 目标与范围

- 本文件适用于整个项目；更深目录的 `AGENTS.md` 可以补充模块规则。
- 当前用户的明确要求优先。实现以需求和验收条件为准，不自行扩大范围。
- 核心原则：先理解、再修改；最小实现；证据优先；保持长期任务方向一致。
- 项目后续所有说明性文档统一使用中文，包括 README、需求与设计文档、实施计划、接口和部署说明、测试报告、验收记录及执行账本；代码标识符、协议字段、命令和必须保留的技术专名可以使用英文。

## 开始工作前

1. 复述任务目标、范围、验收条件和明确不做的内容。
2. 阅读与任务直接相关的 README、设计、配置、代码、测试和状态记录。
3. 检查当前工作区和已有修改；用户的未提交改动必须保留。
4. 找到真实入口、调用链和项目已有命令，不猜测参数、版本或运行方式。
5. 信息不足但不影响安全和方向时，采用最小合理假设并说明；会改变方案或范围时再询问用户。

## 长任务纪律

- 非简单任务维护一份简短工作计划，包含目标、当前阶段、待办项和每项验证方式；同一时间只推进一个主要步骤。
- 优先使用项目已有的状态或计划文档，不创建内容重复的管理文件。
- 每完成一个阶段，都要核对原始需求并记录：完成内容、验证证据、遗留风险、下一步。
- 恢复中断任务时，先对照当前源码、测试结果和状态记录；记录与事实冲突时，以当前源码和新验证为准，并明确指出差异。
- 不因任务耗时、输出暂时沉默或上下文变长而自行改变目标、降低验收标准或宣布完成。
- 只有遇到可复现阻塞、需要用户作出会改变方向的决定，或用户明确要求暂停时才停止推进。

## 实施规则

- 只修改实现需求所必需的文件和代码；不顺手重构、改名、格式化无关内容。
- 优先复用项目已有架构、组件、工具和约定，不重复实现已有能力。
- 避免预留式功能、推测性抽象和没有当前需求支撑的扩展点。
- 不为了展示能力增加额外页面、接口、配置、测试框架或依赖。
- 修复问题要定位根因和实际调用路径，不只处理表面症状。
- 新增或修改行为时同步维护必要的测试、配置和文档；不得通过删除有效测试来获得通过结果。
- 未经用户明确授权，不新增生产依赖，不改变公共 API、数据结构、持久化格式或兼容性边界。

## 验证与完成门禁

- 修改前先确定能够证明任务完成的检查方式。
- 优先运行与改动直接相关的最小测试，再根据影响范围执行构建、静态检查、集成测试或真实流程验证。
- Bug 修复必须重新执行原始失败场景；测试偶发通过不能视为修复。
- 不重复运行大量与决策无关的测试，但不能省略与本次修改直接相关的关键验证。
- 没有本轮新鲜验证证据，不得声称“完成”“修复”“通过”或“可用”。
- 局部测试通过只代表局部范围；不得据此宣称整个项目、部署或真实业务流程通过。
- 无法验证时，准确说明未验证项、原因、影响和用户可执行的下一步。

## 安全与变更边界

- 未经明确要求，不删除文件或数据，不重写 Git 历史，不提交、推送、合并或部署。
- 不覆盖用户已有修改，不用破坏性命令清理工作区。
- 不读取、输出、记录或提交与任务无关的密码、令牌、密钥和私密配置。
- 涉及数据库迁移、生产环境、外部系统、不可逆操作或重大兼容性变化时，先说明影响、验证方式和回滚路径，并等待用户授权。

## 交付格式

最终回复保持简洁，并明确列出：

1. 实际修改内容。
2. 执行过的验证及结果。
3. 未验证内容和已知风险。
4. 仍需用户处理的事项；没有则明确说明没有。
